# セットアップ・起動手順

## 1. 依存パッケージのインストール

```bash
pip install -r requirements.txt
```

Windows環境で `PyQt6-WebEngine` のインストールに失敗する場合は、pipのバージョンを
最新にしてから再実行してください（`pip install --upgrade pip`）。

## 2. 起動

```bash
python main.py
```

## 3. 動作確認の流れ

1. 左パネルの「フォルダを読み込み」で、CSVファイルが入ったフォルダを選択
2. 列マッピング設定でヘッダーなしCSVの場合は列名・単位を設定して「マッピングを適用」
3. 「表示項目」で表示したい列を選択（GPS列がある場合は自動的に地図表示が有効化）
4. 「グラフ表示モード」で通常/日変化/頻度分析を切り替え
5. 通常モードではグラフ上をドラッグすると、その範囲の統計・相関係数が再計算される
6. 「グラフ外観設定」で列ごとの色・軸ラベル・目盛り間隔をカスタマイズ
7. 「出力」でクリップボードへのコピー、または印刷（PDF出力含む）

## 既知の制約・今後の対応予定

- **タイル画像の表示にはインターネット接続が必要**です（OpenStreetMapから取得するため）。
  Leaflet本体（JS/CSS/マーカー画像）はローカル同梱済みのため、地図の枠組み自体は
  オフラインでも初期化されます。
- **PyInstallerによる単一EXE化は未実施**です。次のステップとして、
  `app/gui/right_panel/map_assets/` をdatasとして同梱する`.spec`ファイルの作成が必要です。
- **EMF出力は非対応**です（Windows固有形式でクロスプラットフォームなQt APIから生成できないため）。
  代わりに高解像度PNG＋SVGでのクリップボードコピーに対応しています。
- 複数列の同時重ね描き（現在は1列表示のみ）は未実装です。
- 日変化モードでのグラフ⇔地図同期、および範囲選択からの統計再計算は、
  日付情報を持たないため対象外としています（通常モードのみ対応）。

## 動作確認環境

開発時のテストはLinux + PyQt6 (offscreen platform) で実施しています。
Windows/macOSの実機での動作確認はまだ行っていないため、
フォントやダイアログの見え方等で差異が出る可能性があります。
